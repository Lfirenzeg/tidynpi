% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npi_enrich.R
\name{npi_enrich}
\alias{npi_enrich}
\title{Enrich/match input names against the public lake snapshot}
\usage{
npi_enrich(
  inputs,
  con,
  man,
  strategy = c("strict", "permissive"),
  city_mode = c("prefer", "ignore", "require"),
  max_candidates = 20,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{inputs}{data.frame/tibble with full_name + state (and optional city)}

\item{con}{DuckDB connection (tnp_duckdb())}

\item{man}{Manifest object (tnp_manifest())}

\item{strategy}{"strict" uses jw>=0.95; "permissive" uses jw>=0.90}

\item{city_mode}{"prefer" ranks with city when available; "ignore" ignores city; "require" filters to city matches when both sides have city}

\item{max_candidates}{cap candidates per input_id}

\item{verbose}{Logical. If TRUE, prints progress messages while processing each (state, initial) block.}

\item{...}{passed through to tnp_lake_read_https (e.g., max_urls_per_query, tries, backoff params)}
}
\description{
Enrich/match input names against the public lake snapshot
}
