% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake_search_https.R
\name{tnp_lake_read_https}
\alias{tnp_lake_read_https}
\title{Read from the public HTTPS lake with batching + retries (rate-limit friendly)}
\usage{
tnp_lake_read_https(
  con,
  man,
  state,
  initials = NULL,
  where_sql = NULL,
  columns = c("npi", "first_name", "last_name", "state"),
  max_urls_per_query = 2,
  tries = 6,
  initial_wait = 0.8,
  max_wait = 8,
  sleep_between_batches = 0.3,
  verbose = interactive()
)
}
\arguments{
\item{con}{DuckDB connection (from \code{tnp_duckdb()}).}

\item{man}{Manifest list (from \code{tnp_manifest()}).}

\item{state}{Two-letter state (e.g., "NY").}

\item{initials}{Optional vector of initials to include (e.g., c("S","M")); NULL = all.}

\item{where_sql}{Optional SQL predicate to push down (e.g., "entity_type = 1").}

\item{columns}{Character vector of columns to select.}

\item{max_urls_per_query}{How many Parquet files to read per SQL call.}

\item{tries}{Max retries per query if rate limited.}

\item{initial_wait}{First backoff wait.}

\item{max_wait}{Max backoff wait.}

\item{sleep_between_batches}{Pause between batches to be gentle.}

\item{verbose}{Print progress / retry messages.}
}
\value{
A data.frame with the bound results (0 rows if no matches).
}
\description{
Read from the public HTTPS lake with batching + retries (rate-limit friendly)
}
